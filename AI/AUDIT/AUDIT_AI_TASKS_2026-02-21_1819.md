# AUDIT AI_TASKS — ADR007 Improvements

- Data/ora (UTC): 2026-02-21 18:19
- Branch: `develop`
- Ultimo commit: `c632ead`

## Commit creati (ordine esecuzione)
- `336c88f` chore(ai): clean repo state and align ADR007 task set
- `1832240` docs(decisions): clarify lockfile handling and add exit criteria for ADR 007
- `6c3f106` chore(build): add npm install flags and log node/npm versions in FE stage
- `c632ead` chore(ci): verify ADR007 mitigations on arm64 build

## Esito per step

### STEP 001 — ADR 007 lockfile policy + exit criteria
- Esito: DONE
- File toccati:
  - `AI/DECISIONS.md`
  - `AI/AI_TASKS.md`
  - `AI/KNOWLEDGE.yaml`
- Sintesi:
  - Esplicitato che `package-lock.json` viene rimosso solo nello stage FE del container.
  - Aggiunti 2 exit criteria verificabili per rientro a `npm ci` deterministico.
  - Chiarito che il workaround non impatta runtime add-on.
- Comandi:
  - `git diff -- AI/DECISIONS.md`

### STEP 002 — Harden stage FE + version logging
- Esito: DONE
- File toccati:
  - `file_editor_plus/Dockerfile`
  - `AI/CONTEXT/issue_17_rollup_musl.md`
  - `AI/AI_TASKS.md`
  - `AI/KNOWLEDGE.yaml`
- Sintesi:
  - Stage FE aggiornato con `RUN node -v && npm -v`.
  - Install aggiornata a `npm install --include=optional --no-audit --no-fund`.
  - Contesto aggiornato con versioni di riferimento (`node v20.20.0`, `npm 10.8.2`).
- Comandi:
  - `git diff`
  - `docker run --rm node:20-bookworm-slim sh -lc 'node -v && npm -v'`

### STEP 003 — Verifica arm64 mitigazioni
- Esito: DONE
- File toccati:
  - `AI/CONTEXT/issue_17_rollup_musl.md`
  - `AI/AI_TASKS.md`
  - `AI/KNOWLEDGE.yaml`
- Sintesi:
  - Build arm64 FE verificata con `--no-cache --pull`, esito OK.
  - Log confermano stampa di `node -v` e `npm -v` nello stage FE.
  - Evidenze integrate nel contesto tecnico issue.
- Comandi:
  - `docker pull --platform linux/arm64 node:20-bookworm-slim`
  - `docker build --no-cache --pull --platform linux/arm64 --target fe -f file_editor_plus/Dockerfile file_editor_plus`

## Test eseguiti (run corrente)
- `docker run --rm node:20-bookworm-slim sh -lc 'node -v && npm -v'` → PASS
- `docker build --no-cache --pull --platform linux/arm64 --target fe -f file_editor_plus/Dockerfile file_editor_plus` → PASS

## Problemi latenti / TODO
- Builder Docker legacy senza buildx puo' introdurre errori di cache cross-platform (mismatch arm64/amd64); mitigato con `--pull --no-cache`.
- Warning Dockerfile `InvalidDefaultArgInFrom` su `BUILD_FROM` resta backlog separato.
- Workaround lockfile nello stage FE resta non deterministico rispetto a `npm ci` finche' non scattano gli exit criteria ADR007.

