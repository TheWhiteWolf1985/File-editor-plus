# Audit AI_TASKS — Rollup arm64/musl build fix

- Data/ora (UTC): 2026-02-21 12:25
- Branch: `develop`
- HEAD: `32dabc1`
- Tag: `v1.0.0` (annotated)

## Commit creati (in ordine)
- `bb68922` chore(ai): capture issue 17 rollup musl build context
- `5cf13fe` fix(docker): include optional deps in FE build (rollup musl arm64)
- `07b37b9` chore(ci): verify fix on arm64 musl
- `213bc53` chore(frontend): harden npm optional deps install via npmrc
- `7b12bc0` chore(ci): verify npmrc hardening on arm64 musl
- `5c925f6` docs: track BUILD_FROM warning backlog
- `e65d370` fix(docker): use debian slim for FE build stage to avoid musl rollup issues
- `006e0b5` docs(decisions): record rationale for FE base image fallback
- `5b2c6a8` chore(qa): complete smoke checks for rollup musl fix
- `32dabc1` chore(release): prepare 1.0.0 with arm64 musl build fix

## Stato step

### STEP 001 — Contesto riproduzione
- Esito: DONE
- Output: `AI/CONTEXT/issue_17_rollup_musl.md`
- Nota: errore riprodotto su `linux/arm64` con `node:20-alpine` (`@rollup/rollup-linux-arm64-musl` mancante).

### STEP 002 — Patch 1: include optional deps nello stage FE
- Esito: DONE
- Output: `file_editor_plus/Dockerfile` (`npm ci --include=optional`)

### STEP 003 — Verifica Patch 1 su arm64/musl
- Esito: DONE (FAIL atteso: la patch non risolve)
- Evidenza: `AI/CONTEXT/issue_17_rollup_musl.md`

### STEP 004 — Patch 2: `.npmrc include=optional`
- Esito: DONE
- Output: `file_editor_plus/frontend/.npmrc`

### STEP 005 — Verifica hardening su arm64/musl
- Esito: DONE (FAIL atteso: hardening non risolve in Docker stage con lockfile)
- Evidenza: `AI/CONTEXT/issue_17_rollup_musl.md`

### STEP 006 — Backlog warning `BUILD_FROM`
- Esito: DONE
- Output: backlog documentato in `AI/CONTEXT/issue_17_rollup_musl.md` (warning non bloccante).

### STEP 007 — Fallback: base image FE su Debian slim + workaround lockfile
- Esito: DONE
- Output: `file_editor_plus/Dockerfile`
- Verifica: `docker build --platform linux/arm64 --target fe -f file_editor_plus/Dockerfile file_editor_plus` OK.

### STEP 008 — Decision log
- Esito: DONE
- Output: `AI/DECISIONS.md` (ADR 007).

### STEP 009 — Smoke checklist
- Esito: DONE
- Output: `AI/CHECKLISTS/SMOKE.md`
- Eseguiti: FE build, BE unit tests via Docker, add-on rebuild+restart, check log boot (uvicorn up).

### STEP 010 — Release v1.0.0
- Esito: DONE
- Output:
  - `file_editor_plus/config.yaml` (versione gia' `1.0.0`)
  - `file_editor_plus/CHANGELOG.md` (nota fix arm64)
  - `AI/CHECKLISTS/RELEASE.md` (compilata)
  - tag `v1.0.0`

## Comandi principali eseguiti (estratto)
- Frontend:
  - `cd file_editor_plus/frontend && npm ci && npm run -s typecheck && npm run build` (OK)
- Backend:
  - `docker run --rm -v "$PWD/file_editor_plus/backend:/app" -w /app python:3.12-alpine sh -lc "python -m pip install -r requirements.txt >/dev/null && python -m unittest -q"` (OK)
- Docker (stage FE):
  - `docker build --platform linux/arm64 --target fe -f file_editor_plus/Dockerfile file_editor_plus` (OK)
  - `docker build --no-cache --pull --platform linux/amd64 --target fe -f file_editor_plus/Dockerfile file_editor_plus` (OK)
- Add-on:
  - `docker exec hassio_cli ha apps rebuild local_file_editor_plus` (OK)
  - `docker exec hassio_cli ha apps restart local_file_editor_plus` (OK)
- Release:
  - `git tag -a v1.0.0 -m "v1.0.0"` (OK)

## Problemi latenti / TODO (P0/P1)
- P0: stage FE usa workaround `rm package-lock.json && npm install` (meno deterministico di `npm ci`). Motivazione e contesto in `AI/DECISIONS.md` (ADR 007) e `AI/CONTEXT/issue_17_rollup_musl.md`.
- P1: warning Dockerfile `InvalidDefaultArgInFrom` (ARG `BUILD_FROM` senza default) ancora presente: backlog in `AI/CONTEXT/issue_17_rollup_musl.md`.
- P1: builder legacy (no buildx/BuildKit) rende `--platform` piu' fragile (cache cross-arch). Se disponibile, installare buildx (fuori scope degli step).

