# AUDIT — AI_TASKS (Save As + Cloud Backup)

- Data (UTC): 2026-02-19
- Branch: `develop`
- HEAD (prima audit commit): `0bb7407`

## Commit creati per STEP (in ordine)
- STEP 002: `c49f71d` fix(ui): implement real save-as (folder + filename)
- STEP 003: `ea1df4c` fix(backend): support save-as via atomic write with overwrite control
- STEP 004: `a8c8dd4` chore(cloud): decide oauth credential source (ha vs addon options)
- STEP 005: `5f509ff` feat(cloud): google drive device auth and zip backup upload
- STEP 006: `a610bc0` feat(cloud): scheduled drive backups with retention for auto only
- STEP 007: `c2fb828` feat(ui): google drive backup screen (device auth + schedule)
- STEP 009: `0bb7407` docs(audit): update nav audit for save-as and cloud backup

## STEP 001 — Discovery
- Esito: DONE (no commit richiesto)
- Output: mappati `apiSaveFile`/`apiCreateFile` e `PUT /api/file?create_only=1`, plus download endpoint `GET /api/fs/download`.

## STEP 002 — Save As UI (dialog + conflitto)
- Esito: DONE
- File toccati:
  - `file_editor_plus/frontend/src/app-root.ts`
- Note:
  - Save As ora crea un nuovo file (cartella+nome) usando `apiCreateFile()`; su conflitto mostra prompt a 3 scelte (Sì/No/Cancel) e fa switch al nuovo file.
- Verifiche:
  - `npm run -s typecheck` OK
  - `npm run -s build` OK

## STEP 003 — Backend conflict semantics
- Esito: DONE
- File toccati:
  - `file_editor_plus/backend/app.py`
- Note:
  - `create_only` ora ritorna `409` su conflitto (prima `400`).

## STEP 004 — Decisione credenziali OAuth
- Esito: DONE
- File toccati:
  - `file_editor_plus/config.yaml`
  - `AI/DECISIONS.md` (ADR 006)
- Decisione:
  - Nessun `custom_components/` -> credenziali via add-on options: `gdrive_client_id`.

## STEP 005 — Backend Google Drive (device flow + backup manual)
- Esito: DONE
- File toccati:
  - `file_editor_plus/backend/app.py`
- Endpoint aggiunti:
  - `GET  /api/cloud/gdrive/status`
  - `POST /api/cloud/gdrive/device/start`
  - `POST /api/cloud/gdrive/device/cancel`
  - `POST /api/cloud/gdrive/disconnect`
  - `POST /api/cloud/gdrive/backup`
- Storage:
  - `/data/gdrive/tokens.json`
  - `/data/gdrive/config.json` (folder_id)

## STEP 006 — Schedule + retention (solo auto)
- Esito: DONE
- File toccati:
  - `file_editor_plus/backend/app.py`
- Endpoint aggiunti:
  - `GET /api/cloud/gdrive/schedule`
  - `PUT /api/cloud/gdrive/schedule`
- Note:
  - Worker thread avviato on-demand quando la schedule viene abilitata.
  - Auto backup usa filename `config-auto-*.zip`; retention applicata solo a questi (best-effort).

## STEP 007 — UI Google Drive Backup
- Esito: DONE
- File toccati:
  - `file_editor_plus/frontend/src/app-root.ts`
  - `file_editor_plus/frontend/src/services/api.ts`
  - `file_editor_plus/frontend/src/i18n/*.json` (aggiornato desc cloud)
- Note:
  - Abilitato `Backup -> Cloud`: apre modal con connect device flow (polling), backup manuale, schedule settings e disconnect.

## STEP 008 — Smoke add-on
- Esito: DONE (no commit richiesto)
- Comandi:
  - `ha apps update ...` -> "No update available" (previsto: nessun bump versione in questo epic)
  - `ha apps restart ...` OK
- Note:
  - Supervisor logs mostrano errore Ingress transitorio durante restart (connessione WS), poi recovery.

## STEP 009 — Audit/Knowledge update
- Esito: DONE
- File toccati:
  - `AI/AUDITS/UI_NAV_AUDIT.md`
  - `AI/KNOWLEDGE.yaml`
- Note:
  - Audit aggiornato: Save As non e' piu' download; Backup Cloud non e' piu' NOT CONNECTED.

## Test eseguiti (complessivi)
- Frontend:
  - `cd file_editor_plus/frontend && npm ci && npm run -s typecheck`
  - `cd file_editor_plus/frontend && npm ci && npm run -s build`
- Backend:
  - `docker run --rm -v "$PWD/file_editor_plus/backend:/app" -w /app python:3.12-alpine sh -lc "python -m pip install -r requirements.txt >/dev/null && python -m unittest -q"`

## Problemi latenti / TODO / rischi
- Add-on update in HA dev richiede bump versione in `file_editor_plus/config.yaml` per rendere `ha apps update` disponibile.
- Cloud backup: token refresh e retention sono best-effort; eventuali errori Google Drive sono riportati come `502` (vedere messaggio) e potrebbero richiedere UX piu' guidata.
- Scheduler: thread avviato on-demand; se l'add-on viene riavviato, la schedule resta su file ma il thread riparte solo quando si fa una `PUT schedule` (scelta conservativa).
